<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.hse.egorov.reading_tracker">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.SplashActivity"
            android:screenOrientation="portrait"
            android:theme="@style/SplashScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.login.SignInActivity"
            android:label="@string/title_activity_sign_in"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="stateVisible|adjustResize"/>
        <activity
            android:name=".ui.login.SignUpActivity"
            android:label="@string/title_activity_sign_up"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ui.session.ReadingSessionActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".ui.session.EndOfSessionActivity" />
        <activity android:name=".ui.book_library.BookLibraryActivity" />
        <activity android:name=".ui.book_library.BookInfoActivity" />
        <activity android:name=".ui.session.SessionLibraryActivity" />
        <activity android:name=".ui.session.SessionInfoActivity" />
        <activity android:name=".ui.AllTimeStatisticsActivity" />
        <activity android:name=".ui.book_library.AddingBookActivity" />
        <activity
            android:name=".ui.login.SignUpPersonalInfoActivity"
            android:windowSoftInputMode="stateVisible|adjustResize"></activity>
    </application>

</manifest>